# DD Prime — Enterprise 2026

Versão reconstruída do zero.

## Publicar

Copie os arquivos para `ddprime-site-v1` e execute:

```powershell
npm.cmd install
npm.cmd run build
git add .
git commit -m "Publica versão Enterprise 2026"
git push
```
